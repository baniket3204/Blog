function HowTo(){
    return(
        <div>
            Open development environment and run the command <b>npx json-server --watch data/db.json --port 8000 </b>
              to connect db.json to the project.Also refer to read me file in the documentation.
        </div>
    );
}

export default HowTo;